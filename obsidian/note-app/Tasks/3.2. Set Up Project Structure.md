
# Set Up Project Structure

## Prerequisites
- Vue.js application initialized (reference: Task 3.1)
- Node.js and npm installed
- Project dependencies installed

## Steps

### 1. Create Directory Structure
- [x] Verify you're in the frontend directory:
  ```bash
  cd frontend
  ```

- [x] Create required directories:
  ```bash
  mkdir -p src/components
  mkdir -p src/views
  mkdir -p src/services
  mkdir -p src/assets
  ```

### 2. Set Up Router Configuration
- [x] Update router configuration in `src/router/index.js`:
  ```javascript:src/router/index.js
  import { createRouter, createWebHistory } from 'vue-router'
  import Home from '../views/Home.vue'
  import Login from '../views/Login.vue'
  import Register from '../views/Register.vue'
  import Notes from '../views/Notes.vue'

  export const routes = [
    {
      path: '/',
      name: 'Home',
      component: Home
    },
    {
      path: '/login',
      name: 'Login',
      component: Login
    },
    {
      path: '/register',
      name: 'Register',
      component: Register
    },
    {
      path: '/notes',
      name: 'Notes',
      component: Notes,
      meta: { requiresAuth: true }
    }
  ]

  const router = createRouter({
    history: createWebHistory(process.env.BASE_URL),
    routes
  })

  // Navigation guard for protected routes
  router.beforeEach((to, from, next) => {
    const isAuthenticated = localStorage.getItem('token')
    
    if (to.meta.requiresAuth && !isAuthenticated) {
      next('/login')
    } else {
      next()
    }
  })

  export default router
  ```

### 3. Create Initial View Components
- [ ] Create placeholder view components:

  **src/views/Home.vue**:
  ```vue
  <template>
    <div class="home">
      <h1>Welcome to Notes App</h1>
    </div>
  </template>

  <script>
  export default {
    name: 'Home'
  }
  </script>
  ```

  **src/views/Login.vue**:
  ```vue
  <template>
    <div class="login">
      <h2>Login</h2>
    </div>
  </template>

  <script>
  export default {
    name: 'Login'
  }
  </script>
  ```

  **src/views/Register.vue**:
  ```vue
  <template>
    <div class="register">
      <h2>Register</h2>
    </div>
  </template>

  <script>
  export default {
    name: 'Register'
  }
  </script>
  ```

  **src/views/Notes.vue**:
  ```vue
  <template>
    <div class="notes">
      <h2>My Notes</h2>
    </div>
  </template>

  <script>
  export default {
    name: 'Notes'
  }
  </script>
  ```

### 4. Update App.vue Template
- [x] Modify `src/App.vue`:
  ```vue
  <template>
    <div id="app">
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
          <router-link class="navbar-brand" to="/">Notes App</router-link>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
              <li class="nav-item">
                <router-link class="nav-link" to="/">Home</router-link>
              </li>
              <li class="nav-item">
                <router-link class="nav-link" to="/login">Login</router-link>
              </li>
              <li class="nav-item">
                <router-link class="nav-link" to="/register">Register</router-link>
              </li>
            </ul>
          </div>
        </div>
      </nav>
      <main class="container mt-4">
        <router-view></router-view>
      </main>
    </div>
  </template>

  <script>
  export default {
    name: 'App'
  }
  </script>

  <style>
  #app {
    font-family: Avenir, Helvetica, Arial, sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    color: #2c3e50;
  }
  </style>
  ```

### 5. Verification Steps
- [x] Start the development server:
  ```bash
  npm run serve
  ```
- [x] Verify all routes are working:
  - Visit http://localhost:8080/
  - Test navigation between pages
  - Check responsive navbar behavior
  - Verify Bootstrap styles are applied

## References
- Vue.js Application Setup:
```markdown:obsidian/note-app/Tasks/3.1. Initialize Vue.js Application.md
startLine: 13
endLine: 28
```

- Frontend Development Epic:
```markdown:obsidian/note-app/Epics/Epic 3 Frontend Development.md
startLine: 12
endLine: 16
```

## Next Steps
1. Implement global styles
2. Create navigation components
3. Develop authentication views
4. Handle authentication state

## Notes
- Keep components modular and reusable
- Follow Vue.js best practices
- Use Bootstrap classes for responsive design
- Consider adding TypeScript for better type safety
```

This refactored version provides a clear, step-by-step guide for setting up the project structure, including the creation of necessary directories, router configuration, and initial component templates. It follows Vue.js best practices and integrates with Bootstrap for styling.