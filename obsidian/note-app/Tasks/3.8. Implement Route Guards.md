I'll help you refactor the "Implement Route Guards" task with a clear step-by-step approach using prompts for GPT.

# Implement Route Guards

## Prerequisites
- Vue.js application initialized
- Router configured
- Authentication state management implemented

## Steps

### 1. Update Router Configuration
- [x] Use existing file: `frontend/src/router/index.js`
- [x] Prompt for router guard implementation:
```prompt
"Create a navigation guard for Vue Router that:
1. Checks for protected routes using meta.requiresAuth
2. Verifies authentication status using localStorage token
3. Redirects unauthorized users to login
4. Handles navigation to login/register when already authenticated
5. Implements proper error handling
6. Uses composition API style
7. Includes TypeScript types (optional)

The guard should integrate with the existing router configuration and auth store."
```
Reference existing router setup:
```javascript:frontend/src/router/index.js
startLine: 34
endLine: 56
```

### 2. Create Auth Guard Utility
- [x] Create new file: `frontend/src/utils/authGuard.js`
- [x] Prompt for auth utility:
```prompt
"Create an authentication utility that:
1. Provides functions to check auth status
2. Validates JWT tokens
3. Handles token expiration
4. Includes proper TypeScript types (optional)
5. Uses composition API
6. Implements proper error handling
7. Exports reusable auth check functions"
```

### 3. Update Navigation Component
- [x] Use existing file: `frontend/src/components/NavBar.vue`
- [x] Prompt for navigation updates:
```prompt
"Update the navigation component to:
1. React to authentication state changes
2. Show/hide protected route links
3. Handle logout functionality
4. Update UI based on current route
5. Include proper TypeScript types (optional)
6. Use composition API
7. Implement smooth transitions"
```
Reference existing NavBar implementation:
```vue:frontend/src/components/NavBar.vue
startLine: 11
endLine: 116
```

### 4. Add Protected Route Wrapper
- [x] Create new file: `frontend/src/components/ProtectedRoute.vue`
- [ ] Prompt for protected route component:
```prompt
"Create a ProtectedRoute component that:
1. Wraps protected route components
2. Handles authentication checks
3. Shows loading states during checks
4. Redirects unauthorized access
5. Includes proper TypeScript types (optional)
6. Uses composition API
7. Implements proper error handling"
```

## Verification Steps
- [ ] Test protected route access when logged out
- [ ] Verify redirect to login page
- [ ] Test protected route access when logged in
- [ ] Verify navigation guard prevents unauthorized access
- [ ] Test logout functionality and redirects
- [ ] Verify proper handling of direct URL access

## Notes
- Keep authentication checks consistent across components
- Consider implementing token refresh logic
- Add proper error handling and user feedback
- Consider caching auth state for better performance
- Document any security considerations